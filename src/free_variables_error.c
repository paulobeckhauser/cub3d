

#include "../../inc/cub3d.h"

int free_variables_error(t_data *data)
{
    // free_2d_array(data->map);
    // if (data->map)
    //     free_2d_array(data->map);
    (void )data;
    
    return(1);
}